# Sad2Key 引き継ぎ書

## 目的

Sad2Keyは，Bluetooth接続されたNintendo Switch Pro Controller / Joy-Conなどの入力を読み取り，キーボード入力へ変換するWindows向けC# WinFormsアプリです．

JoyToKeyで発生していた接続不安定や入力変換不具合を回避するため，Switch系コントローラー向けにHID直接読み取り経路を追加しています．

## プロジェクト場所

- 作業ディレクトリ: `C:\Users\catso\Downloads\JoyToKey\Sad2Key`
- ソリューション: `C:\Users\catso\Downloads\JoyToKey\Sad2Key\Sad2Key.sln`
- プロジェクト: `C:\Users\catso\Downloads\JoyToKey\Sad2Key\Sad2Key\Sad2Key.csproj`
- Release実行ファイル: `C:\Users\catso\Downloads\JoyToKey\Sad2Key\Sad2Key\bin\Release\net8.0-windows10.0.19041.0\Sad2Key.exe`

## 作成ルール

`C:\Users\catso\Downloads\JoyToKey\Sad2Key\作成ルール.txt` を参照しています．

反映済み:

- 関数上部に処理説明コメントを付与
- 作成者を `C2H4` に設定
- 会社名は未設定
- コードコメントの句読点は `，` と `．` を使用

## 主なファイル

- `Form1.cs`
  - UI
  - デバイス検出
  - プロファイル一覧
  - JoyToKey形式cfg読み込み
  - cfg新規作成
  - cfg保存
  - マッピング行ダブルクリック編集
  - キー送信
  - 入力監視ループ
- `SwitchHidController.cs`
  - Nintendo Switch系コントローラーのHID直接読み取り
  - HID初期化
  - HIDレポート解析
  - JoyToKey互換の `ButtonNN` 別名出力
- `HidApi.cs`
  - `hidapi.dll` のP/Invoke定義
- `Form1.Designer.cs`
  - フォーム基本サイズ
  - 現在の初期サイズは `860 x 1100`
- `Sad2Key.csproj`
  - .NET 8 Windows WinForms設定
  - `hidapi.dll` の出力コピー設定

## 入力取得方式

優先して使用する経路:

- `Pro Controller (Switch HID)`
- `Joy-Con L (Switch HID)`
- `Joy-Con R (Switch HID)`

補助的な経路:

- `Windows.Gaming.Input`
- `RawGameController`
- `winmm`
- `All detected controllers`

実運用では `All detected controllers` は非推奨です．複数経路を同時に見るため，遅延や重複入力の原因になる可能性があります．

## Switch HID実装

BetterJoyを参考に，Sad2Key向けに必要部分だけを実装しています．

- Nintendo Vendor ID: `0x057E`
- Joy-Con L Product ID: `0x2006`
- Joy-Con R Product ID: `0x2007`
- Pro Controller Product ID: `0x2009`
- `hidapi.dll` でHIDデバイスを列挙
- `0x03 -> 0x30` のサブコマンドで標準入力レポートへ切り替え
- `0x30` / `0x31` レポートを解析
- HIDレポートが一瞬読めない場合は直前入力状態を保持

BetterJoyの仮想XInput，ViGEmBus，HIDGuardian，ジャイロ処理，振動処理は組み込んでいません．

## キー送信

キー送信はWin32 APIの `SendInput` を使用しています．

修正済み:

- 64bit Windowsで `INPUT` 構造体サイズが合わず送信されない問題を修正
- `MouseInput` / `HardwareInput` をunionへ追加
- `SendInput` 失敗時はconsoleへエラーを出力
- `Test Key` ボタンで `A` キー送信確認が可能
- Start後は隠しフォーカス受け皿へフォーカスを移し，Sad2Key自身のUI誤操作を抑制

## 遅延対策

実施済み:

- WinForms UIタイマーからバックグラウンド処理へ変更
- Switch HID選択時はHID読み取り待ち主体に変更
- 入力イベントログは標準OFF
- UI状態更新は約50ms間隔に制限
- リストビューは状態変化時だけ更新
- リストビューのダブルバッファを有効化

運用推奨:

- `Switch HID` の個別コントローラーを選択
- `Input event log` はOFF
- `All detected controllers` は使わない

## UI仕様

現在の画面構成:

- 上部
  - `Controller`
  - `Refresh`
  - `Start`
  - `Stop`
  - `Test Key`
  - `Config`
  - `Input event log`
  - 現在選択中のProfile表示
- 中央左
  - Profileリスト
  - `New`
  - `Save`
  - `Folder`
  - `Reload`
- 中央右
  - 入力名と割り当てキーのマッピング一覧
  - 押下中の行は黄色表示
  - `UP` / `Down` 文字表示は廃止
- 下部
  - consoleログ

Start中はProfileリストとProfile操作ボタンを無効化しています．これは，送信された上下左右キーやEnterキーでSad2Key自身のUIが誤操作されることを防ぐためです．

## Profile機能

JoyToKey形式の `.cfg` を読み込み・作成・保存できます．

実装済み:

- `Folder` でcfg格納フォルダを選択
- 選択フォルダは `%APPDATA%\Sad2Key\settings.json` に保存
- 次回起動時も前回選択フォルダを使用
- `Reload` でcfg一覧を再読込
- `New` で空のcfgプロファイルを作成
- `Save` で現在のマッピングを保存
- `keymap.json` も従来通り選択可能

既定のcfg探索:

- 起動時に設定ファイルがない場合，Sad2Key実行場所から親ディレクトリをたどり，最初に `*.cfg` が見つかったフォルダを使用
- 現状の想定は `C:\Users\catso\Downloads\JoyToKey`

## マッピング編集

マッピング一覧の行をダブルクリックするとキー割り当て編集画面が開きます．

編集画面の仕様:

- キーを押すと割り当てとして取得
- `Ctrl+Z` のような複数キーも取得可能
- `Clear` で割り当て削除
- `OK` で反映
- `Save` でcfgへ保存

現在の入力動作は基本的に長押し型です．コントローラーを押すとキーDown，離すとキーUpです．JoyToKeyのような「一定時間だけ押す」「連射」「特殊シーケンス」はまだ未実装です．

## JoyToKey形式cfgの読み書き仕様

読み込み対応:

- `Button01=1, 53:00:00:00, ...`
- `POV1-1=1, 26:00:00:00, ...`
- `11:5A:00:00` のような複数キー

保存対応:

- `[General]`
- `[Joystick 1]`
- `ButtonNN=1, xx:xx:xx:xx, 0.000, 0, 0`
- `POV1-1/3/5/7=1, xx:xx:xx:xx, 0.000, 0, 0`

未対応:

- `ButtonNN=7, ...` の特殊設定
- 連射
- マウス移動・クリック
- 入力シーケンス
- アプリ別自動切替

## JoyToKeyボタン対応

Switch HID側ではJoyToKey互換の `ButtonNN` 別名を出しています．

- `SwitchB` -> `Button01`
- `SwitchA` -> `Button02`
- `SwitchY` -> `Button03`
- `SwitchX` -> `Button04`
- `SwitchL` -> `Button05`
- `SwitchR` -> `Button06`
- `SwitchZL` -> `Button07`
- `SwitchZR` -> `Button08`
- `SwitchMinus` -> `Button09`
- `SwitchPlus` -> `Button10`
- `SwitchStick` -> `Button11`
- `SwitchRightStick` -> `Button12`
- `SwitchHome` -> `Button13`
- `SwitchCapture` -> `Button14`

右側ボタンのHID参照位置は修正済みです．Pro Controllerでは `report[3]` 側を参照します．

## 使い方

1. Sad2Keyを起動
2. `Folder` でcfgフォルダを選ぶ
3. 左のProfileリストからcfgを選ぶ
4. `Controller` で `Pro Controller (Switch HID)` などを選ぶ
5. 必要ならマッピング行をダブルクリックしてキー割り当てを変更
6. 必要なら `Save` で保存
7. `Start` を押す
8. ゲームやメモ帳など入力先へフォーカスする

## ビルド方法

PowerShellで以下を実行します．

```powershell
cd C:\Users\catso\Downloads\JoyToKey\Sad2Key
dotnet build Sad2Key.sln -c Release
```

直近の確認結果:

- Releaseビルド成功
- 警告0
- エラー0

## 現在確認できている良い点

- Switch HID経路で接続と入力変換が安定
- 入力ラグは実用上かなり改善
- ボタン長押し時の高速連打問題は改善
- Profile UIは左リスト化済み
- マッピング押下行は黄色表示
- cfgフォルダ指定が可能
- cfg新規作成と保存が可能

## 残課題

- JoyToKey形式cfgの完全互換ではありません
- `ButtonNN=7, ...` の特殊設定は未対応
- ボタンごとの「一瞬押し」「長押し」「連射」切替は未実装
- マウス操作割り当ては未実装
- アプリ別Profile自動切替は未実装
- Joy-Con左右合体時の扱いは最低限です
- cfg保存時に未対応設定は保持されず，Sad2Keyが出力できる単純形式へ再生成されます

## 次に実装するとよい内容

- ボタンごとの入力モードを追加
  - Hold
  - Tap
  - Turbo
- マウス移動・クリック割り当て
- JoyToKey特殊設定 `type=7` の解析
- Profile保存時に既存cfgの未対応行を保持する処理
- マッピング編集画面をJoyToKeyに近いタブ構成へ拡張
- 入力テスト画面を追加
- 選択中Profileを次回起動時に復元

